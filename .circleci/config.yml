version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.8
    steps:
      - checkout
      - run:
          name: Echo this is a test
          command: echo $RDS_NAME

workflows:
  build_and_test:
    jobs:
      - build:
        context: aws_dev

  
